/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  // Produce a static export in the `out` folder (replaces `next export`)
  output: "export",
};

export default nextConfig;
